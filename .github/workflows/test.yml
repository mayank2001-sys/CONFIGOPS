# Generated by ConfigOps UI. Edits made here are overwritten on the next push.
name: Run OpKey CLI

on:
  workflow_dispatch:

jobs:
  run-cli:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Run OpKey Task
        run: >
          java -jar "OpKeyCLI-0.6-jar-with-dependencies.jar"
          --hostname "https://qa1.perf-eks.opkeyone.com/"
          --username "${{ secrets.OPKEY_USERNAME }}"
          --apikey "${{ secrets.OPKEY_API_KEY }}"
          --project "jira_configops"
          --module devopstask
          --command createApplyconfigurationTask
          --task-name "ApplyConfigurations_2026_09_24 12_44_18_611"
          --agent-id "00000000-0000-0000-0000-000000000000"
          --requirement-id "00000000-0000-0000-0000-000000000000"
          --parent-id "00000000-0000-0000-0000-000000000000"
          --environment-id "2b550ab2-ce4e-47b6-a55f-8a3841a01675"
          --module-name "Financials"
          --sub-module "General Ledger"
          --task-files "FUN_MANAGE_BUSINESS_UNIT"
          --reporter-id "eb38792e-3180-41ea-ac00-017646064392"
